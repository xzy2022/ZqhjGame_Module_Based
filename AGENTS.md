
本项目源自红枫2026挑战赛的官方项目，包含赛题一、赛题二和赛题三的代码和文档。

但是我参加的是赛题二多机协同识别的参赛，因此你应该主要关注项目的赛题二部分。

本地开发的平台是windows11，我拉取的官方提供的项目是windows+UE版本，到时候也会按照这个版本提交。

开发代码时，注释请使用中文写。每一次对py代码文件修改结束后，用注释在最上方追加写上修改时间、修改的目的和内容(各自是一句话)。
开发代码或规划时，最小化修改，只动核心必做的，而不要花太多时间在错误日志和错误提示的健全上。

当需要了解赛题二的内容时，请看`docs\codex_read\赛道二-多机协同识别介绍.md`
需要了解完整比赛规则时，看`docs\codex_read\红枫2026无人集群自主协同智能算法挑战赛参赛手册.md`

需要了解我的初步规划时，看`docs\human_read\初步规划.md`

当需要启动实验或给出启动实验的命令时，看`docs\human_read\启动方式-自用.md`，路径得设置在`..\output\`下的某个文件夹或者嵌套文件夹。

`..\ZqhjGame`这是我队友共享的项目，对于它我们只运行参考，不要修改。需要更新队友仓库时看`docs\codex_read\更新队友仓库.md`。需要启动队友仓库的基线时看`docs\human_read\启动方式-队友.md`

需要进行排查性开发时，看`docs\codex_read\排查性开发.md`。
需要进行研究性开发时，看`docs\codex_read\研究性开发.md`

需要写测试或者进行测试时，看`docs\codex_read\测试的定义和原则.md`和`docs\human_read\启动方式-自用.md`。
## 独立仓库边界与协作

本目录是 `ZqhjGame_Module_Based` 独立 Git 仓库，算法代码在 `src\personal_hf2026`，场景在 `configs\scenarios`，模型在 `assets`。官方 SDK、UE 和引擎位于父目录；可通过 `run.cmd --sim-root` 和 `--runtime-root` 分别指定官方 SDK 与二进制运行根目录。

首次安装及本仓库启动命令优先看 `docs\human_read\启动方式-给队友.md`。复制的旧文档保留历史说明与结果，只适配路径；如与本仓库运行方式不一致，以新启动文档为准。实验输出必须放在本仓库外的 `..\output\` 下，每轮使用新的目录，不覆盖现有数据集。

本仓库 `main` 从空提交初始化。所有开发必须使用各自独立工作树和 `codex/*` 分支，多智能体协作遵守 `docs\codex_read\研究性开发.md`。完成后只汇总到新的 `codex/*` 集成分支，禁止任何向 `main` 合并的动作，最终合并由用户执行。未经用户明确授权，不推送到远程，不回退或重写原官方仓库历史，不修改队友仓库。