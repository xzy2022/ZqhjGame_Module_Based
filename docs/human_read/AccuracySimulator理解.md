

```bash
引擎掌握目标和诱饵真值
        ↓
根据 UAV/云台视场计算当前是否看到车辆
        ↓
真值只在 runner 内部传递，选手不可见
        ↓
AccuracySimulator：
  - 按概率制造漏检
  - 给坐标加入高斯噪声
  - 根据天气降低识别率、放大噪声
  - 可根据距离进一步降低识别率
        ↓
生成 obs.self.detection
        ↓
你的 Agent 根据这个带误差的结果做决策
```

也就是不需要真的运行yolo，就可以给出一个不是很准、可能会错误的目标识别结果。