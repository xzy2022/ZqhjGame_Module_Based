
## 当前情况说明

直接运行官方基线的`train`模式，分数为0分，见`D:\Workspace\00_MyRepo\red_m_competiton\output\官方基线_train_网页版运行结果.json`

分析原因：
- 当前的无人机被直接放在三个比较远的地方，代码似乎也没有专门同时锁定一个目标的设计，因此无法不满足*盯防摧毁*条件(`docs\codex_read\评分细则.md`)


## 总体规划

1. 先把真目标和诱饵通过认为设计的方式，让模型非常简单的把它们进行区分，开发协同追踪功能，先实现真正的*盯防摧毁*，得到一些分数
2. 然后再相办法开发真目标和诱饵的区分方法，通过视觉/轨迹推测/速度分布差异等方式(尽管我觉得视觉外观的差异可能最靠谱)

之后准备做的：
1. 先大体跑通，对整个任务有个完整的认识，然后分析这整个问题有哪些难点、瓶颈点，然后综合考虑对于得分的影响和难度、时间决策攻克哪些、放弃哪些。
2. 比赛群里主办方说`集中验证环境会与最终评测环境完全一致，跑分结果参考性很强，建议持续刷分`,后续可以根据集中验证的环境分数来反向黑盒推测它的一些特点，以为得分进行针对性优化。

## 目前想法

### 第一阶段 已完成
复制scenario.json一个新的副本，直接为所有的诱饵设置初始化速度为0，真目标保持不变，这样可以直接完全区分诱饵和真目标，然后开发程序，让它先实现多无人机追踪同一个物体，实现一个真目标的消灭，得到一定分数。一直到`9823b01e04f734790525df9dd3854dea06ea1e60`这次提交都是做这件事，已经有不错的结果。

这部分最终开发出了个人PersonalV1基线，后面还有小修小补，但是没这么关键了。

### 第二阶段 进行中

下一步是把队友的真假目标方法(视觉)迁移到我的模型，作为PersonalV2大版本，进行开发。v2的核心目标，是把我当前的速度区分真假目标换成队友的视觉区分真假目标，然后如果在静态诱饵上有不错的效果，把诱饵改成可以移动的。限制：v2仍然保持晴天、真实坐标获取的等等这些理想条件假设。

#### 待解难题

`D:\Workspace\00_MyRepo\red_m_competiton\output\personal_v2\dataset-200s-seed1-20260913-192854-review\annotated_frames\02_decoy_only\20001_f4353_-28613333333.jpg`
我自己拍了图像后感觉有难题，参照agentv1，它的锁定机制要求必须目标是自己最近的才能锁定。因此专门写了原理最大竞争者的逻辑，但是之后如果迁移到使用图像来获得诱饵的坐标，像这种，诱饵显然识别不到，那无人机可能在这附近看到真目标都锁定不了，因为根本不知道竞争者是谁。